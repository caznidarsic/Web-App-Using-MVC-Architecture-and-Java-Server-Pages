# Web App Using MVC Architecture and Java Server Pages
 This web application conforms to the MVC model. The model is a plain old Java object, the view is a JSP and the controller extends HTTPServlet.
